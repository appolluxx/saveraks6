# Custom pg_hba.conf for debugging - Verified
local   all             all                                     trust
host    all             all             0.0.0.0/0               trust
host    all             all             ::/0                    trust
